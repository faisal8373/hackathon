import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    main:{
        width: '1000',
        height: 1000,
        padding: 30,
        margin: 10,
        boxShadow: '10px 10px 10px grey',
        borderRadius: 20,
        backgroundColor: 'rgb(208, 213, 219)',
        
    }
})

export default useStyles;