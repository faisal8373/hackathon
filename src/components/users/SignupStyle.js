import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    signup:{
        marginLeft: 400,
        marginTop: 50,
        width: 450,
        borderRadius: 20,
        padding: 50,
        backgroundColor: 'rgb(200, 222, 247)'
        
    }
});

export default useStyles;